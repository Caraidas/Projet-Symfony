<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Mon site Webtoon{% endblock %}</title>
        <style>
            a {
                text-decoration: none;
                color: inherit;
            }
            a:hover {
                color: #02aab0;
            }
            a:visited, a:active {
                color: inherit;
            }
            body {
                background-color: #020d18;
                color: white;
            }
            header {
                background-color:#0f2133;
                padding: 10px;
                display: flex;
                justify-content: space-between;
                align-items: center;
            }

            .menu-btn {
                padding: 8px 12px;
                background-color: #02aab0;
                color: white;
                text-decoration: none;
                border-radius: 4px;
                margin: 0 3px 6px;
            }

            .menu-btn:hover {
                background-color: #041629;
            }
            .webtoon {
                background-color: #041629;
                border-radius: 10px;
                padding: 15px;
                margin-bottom: 20px;
            }
            .comment-box {
                margin-top: 30px;
                border-radius: 8px;
                padding: 15px;
                background-color:#041629;
            }

            .comment-box textarea {
                width: 99%;
                height: 100px;
                border: none;
                resize: none;
                padding: 10px;
                border-radius: 6px;
                font-size: 14px;
                background-color: white;
                box-shadow: inset 0 0 3px rgba(0,0,0,0.1);
            }

            .comment-box textarea:disabled {
                background-color: #f0f0f0;
                cursor: not-allowed;
            }

            .comment-actions button {
                background: none;
                border: none;
                margin-left: 8px;
                font-size: 16px;
                cursor: pointer;
            }

            .comment-actions button:disabled {
                color: #ccc;
                cursor: not-allowed;
            }

            .send-btn {
                color: white;
                background-color: #36b3b3;
                border-radius: 50%;
                width: 34px;
                height: 34px;
                font-size: 18px;
                display: inline-flex;
                align-items: center;
                justify-content: center;
            }
        </style>
    </head>
    <body class="bg-[#1a1b7f] text-white">       

        <header>
            <div class="logo">
                <a href="{{ path('home') }}"><h1>
                    Cattoon
                </h1></a>
            </div>
            <div class="menu">
                
            
            {% if is_granted('ROLE_AUTHOR') %}
                <a href="{{ path('webtoon_form') }}" class="menu-btn">
                    ✍️ Créer un Webtoon
                </a>
            {% endif %}
            {% if is_granted('ROLE_USER') %}
                <a href="{{ path('user_favoris') }}" class="menu-btn">
                    Mes Favoris
                </a>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('admin_user_list') }}" class="menu-btn">Admin</a>
            {% endif %}
            {% if not app.user %}
                <a href="{{ path('app_login') }}" class="menu-btn">Se connecter</a>
                <a href="{{ path('app_register') }}" class="menu-btn">S'inscrire</a>
            {% else %}
                <a href="{{ path('app_logout') }}" class="menu-btn">Déconnexion</a>
            {% endif %}
            
            </div>
            
        </header>

        <main>
            {% block body %}{% endblock %}
        </main>

    </body>
</html>
